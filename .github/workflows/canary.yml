# DELIBERATELY BROKEN - do not merge this file.
#
# This is the known-bad input that proves lint-workflows catches the class
# that made release.yml uncompilable: an empty ${{ }} expression pair inside
# a run: block. workflow_dispatch-only so GitHub never executes it; actionlint
# still parses it. The next commit reverts this file, and the red -> green
# pair in this PR's checks is the proof the gate is alive.
name: Canary
on: workflow_dispatch
jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ }}"
