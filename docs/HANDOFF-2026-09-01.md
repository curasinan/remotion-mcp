# Handoff — CI hardening, 2026-09-01

Written for a fresh session with no prior context. Everything below was verified
by execution unless explicitly marked otherwise.

**Branch:** `ci/hardening` @ `372e1c8` · **PR:** [#3](https://github.com/curasinan/remotion-mcp/pull/3), ready for review
**Plan executed:** [`docs/superpowers/plans/2026-08-31-ci-hardening.md`](superpowers/plans/2026-08-31-ci-hardening.md) — all 9 tasks complete
**Base:** `main` @ `e6c685f`, now tagged `v1.2.0`

---

## 1. CI status: green

Run [33452161442](https://github.com/curasinan/remotion-mcp/actions/runs/33452161442), **17/17 jobs pass.**

```
build-and-test   ubuntu-latest    22 / 24 / 26      pass
build-and-test   windows-latest   22 / 24 / 26      pass
build-and-test   macos-latest     22 / 24 / 26      pass
bundle                                             pass
sbom                                               pass
verify-bundle    ubuntu-latest        linux-x64-gnu     pass
verify-bundle    ubuntu-24.04-arm     linux-arm64-gnu   pass
verify-bundle    windows-latest       win32-x64         pass
verify-bundle    windows-11-arm       win32-arm64       pass
verify-bundle    macos-latest         darwin-arm64      pass
verify-bundle    macos-15-intel       darwin-x64        pass
```

Local: `npm test` → **103 pass, 0 fail, 0 skipped**. `npm run lint` → 5 advisory findings (listed in §4).

**What `verify-bundle` does, and why it matters.** It downloads the built `.mcpb`,
extracts it on each platform, completes a real MCP handshake against the bundled
server, and calls `viz_render_svg` — which forces the platform's own
`@resvg/resvg-js` native binary to load. Before this job existed, exactly one of
the six shipped binaries was ever loaded, and only incidentally.

This is not theoretical coverage. It was demonstrated: corrupting the
`darwin-arm64` binary inside an otherwise valid `.mcpb` leaves all 10
`test/bundle.test.mjs` assertions **green** while the artifact is unusable for
every macOS ARM user. Only the `macos-latest` leg catches it. Corrupting the
*build platform's own* binary is the one case `bundle.test.mjs` already caught.

---

## 2. What landed

20 commits, 26 files, +10,830 / −1,331.

| Task | Change |
| --- | --- |
| 0 | Repo made public (verified zero secrets across all 45 commits first) |
| 1 | Matrix → Node 22/24/26; `engines: ">=22"`; `test/engines.test.mjs` ties the two together |
| 2 | Coverage via Node's own flag, advisory. Baseline **70.37% lines** |
| 3 | ESLint advisory. 169 → 5 findings after fixing the config |
| 4 | `tools/list` checked by MCP Inspector 2.4.0 — an independent client, asserts exactly 13 tools |
| 5 | Bundle size gate, baseline 20,463,297 bytes (19.52 MiB), +10% ceiling |
| 6 | Six-platform artifact verification (the headline deliverable) |
| 7 | Trivy fs scan (advisory) + SPDX SBOM alongside the existing CycloneDX |
| 8 | `scripts/sync-version.mjs` — moves all three version statements atomically |
| 9 | `release.yml`: semantic-release + cosign keyless signing. **Never executed** |

Plus one out-of-plan commit: `af95225` changed `waitUntil: "networkidle0"` → `"load"`
in `src/services/raster.ts`. See §5.

---

## 3. Next steps, in order

### 3.1 Merge PR #3

`main` has **no branch protection**, which is why a Windows regression went
unnoticed for four commits mid-branch. Consider adding a required status check on
`build-and-test` — but note `@semantic-release/git` pushes to `main`, so the bot
needs an exemption.

**PR #2 (`fix/ci-browser-timeouts`) is superseded.** This branch is stacked on it
and contains all its commits. Close it rather than merging both.

### 3.2 Immediately after merge — close the one unverified claim

`release.yml` has **never compiled on GitHub**. That was a real defect (§5), now
fixed, but the fix is proven *indirectly*: the workflows API still lists it by
path rather than by its `name: Release` key, and it cannot show otherwise from a
feature branch — `release.yml` does not exist on `main`, and GitHub seeds that
entity's name from the default branch.

The substitute evidence is that the broken commit `9b74c35` produced **two** runs
(`ci.yml` plus a zero-job `release.yml` failure) while `6dae0d7` and `372e1c8`
each produced only `ci.yml`. Sound, but it is reasoning, not observation.

```bash
gh api repos/curasinan/remotion-mcp/actions/workflows --jq '.workflows[] | "\(.name) -> \(.path)"'
```

**Expected after merge:** `Release -> .github/workflows/release.yml`. If it still
lists by path, there is a second schema error — read the annotation on the
Actions run page (the API does not expose it).

### 3.3 The first real release

`v1.2.0` is now tagged on `e6c685f`. **Verified live after tagging:** semantic-release
computes `1.2.1` and the downgrade guard permits it. Before the tag it computed
`1.0.0` and the guard correctly refused.

Nothing about the release path has ever run on GitHub. Specifically unverified:

- cosign has never executed. No Fulcio certificate, no Rekor entry.
- `--certificate-identity-regexp` has never matched a real SAN. **Most likely first failure.**
- `@semantic-release/git`'s push to `main`, the ~19.5 MiB asset upload, and `npm ci` with the new devDeps on a runner.

Suggested path: merge → confirm §3.2 → `gh workflow run release.yml` from `main`
and watch it → then verify the published artifact end to end with the recipe in
`README.md` § "Verifying a release".

**Note on cosign versions.** The workflow pins `sigstore/cosign-installer` to
`v3.10.1` (cosign 2.6.1) deliberately. cosign 3's `verify-blob` has **neither
`--certificate` nor `--signature`**, and its `sign-blob` has only `--bundle` — so
a user who installs cosign today cannot verify these artifacts at all. The README
says so. Moving to cosign 3 means rewriting the sign step *and* the README recipe
together.

### 3.4 Three follow-ups the final review recommends

1. **Add `actionlint` to `ci.yml`.** It catches the exact defect that made
   `release.yml` uncompilable — an empty `${{ }}` inside a `run:` block — in
   seconds. That class is currently invisible to this repo's CI, which is why it
   survived a full task review.
2. **Couple `MIN_NODE_MAJOR` into `test/engines.test.mjs`.** The Node floor now
   lives in four places; the test couples `package.json` ↔ `manifest.json` but
   not `src/services/environment.ts`. That is the same drift that produced a
   Critical on this branch.
3. **SHA-pin `aquasecurity/trivy-action@v0.36.0`** (`ci.yml:173`). The other two
   third-party actions are SHA-pinned; this one is still tag-pinned.

### 3.5 Task 3b — flip ESLint to blocking

`npm run lint` is `continue-on-error: true`. Five real findings remain
(§4). Fix them, then remove the flag in its own commit.

---

## 4. Known open items

| Item | Where | Severity |
| --- | --- | --- |
| 3× `no-useless-escape` | `src/schemas/common.ts:24,50`, `src/services/svg.ts:173` | advisory |
| 2× `no-unused-vars` | `test/audit-isolation.test.mjs:138`, `test/audit.test.mjs:6` | advisory |
| `void request.continue()` / `abort()` now have `.catch()`, but an unresolved intercepted request still stays in flight forever | `src/services/raster.ts` | latent |
| `verifyStudioProcess` degrades to `unknown` on a busy Windows machine — `LOOKUP_TIMEOUT_MS` is 5 s and PowerShell cold start can exceed it. `remotion_stop_studio` then fails open | `src/services/process.ts` | **documented intent, but the cliff is now known reachable in practice — the doc comment does not say that** |
| `engines.test.mjs:37` hardcodes `EOL = [18, 20]` — a snapshot, not a rule. Stops protecting silently when 22 EOLs (April 2027) | `test/engines.test.mjs` | latent |
| `package-lock.json` drifts one release behind (`sync-version.mjs` does not write it) | — | hygiene |
| Two `.mcpb` artifact uploads on `main` — 40 MB/run for the same bytes | `ci.yml` | consolidate |
| `darwin-x64` coverage expires ~Aug 2027 with GitHub's last Intel image | `ci.yml` | calendar |
| `scripts/verify-bundle-runtime.mjs` imports from `test/zip-reader.mjs` — layering is backwards | — | tidy |
| `prepareCmd` fetches six resvg tarballs from npm at release time, outside `package-lock.json`. The signature attests *which workflow built the file*, not that its vendored binaries match a locked hash | `scripts/build-bundle.mjs` | document |

---

## 5. Two bugs worth understanding before touching this code

### The macOS hang was never a timeout

`waitUntil: "networkidle0"` does not mean puppeteer counts requests. It waits for
Blink to emit `Page.lifecycleEvent{networkIdle}`, and on the macOS runner Blink
sometimes never emits it.

```
run 33322657864  macos 20/22    9,809ms /  11,470ms   PASS / PASS
run 33323021866  macos 20/22   38,742ms /  44,770ms   FAIL / FAIL
run 33425660434  macos 20/22  101,077ms /   8,517ms   FAIL / PASS
run 33431085855  macos 20/22   98,329ms /  95,131ms   FAIL / FAIL
```

`git diff --stat 73b4e73 e6c685f` is **empty** — the first two runs are
byte-identical trees on the same runner image eight minutes apart, one green one
red. Timing was bimodal (8.5–11.5 s or past the deadline, never between), and
failure time tracked whatever the deadline was (30 s → 38.7 s, 90 s → 98.3 s),
which rules out **any** timeout value as a fix. The number was raised twice
before anyone measured.

Dropping the wait is correct on the merits: `viz_render_html` renders
caller-supplied markup under a deny-all network policy, so every request is
aborted before egress. Waiting for the network to go quiet waits for a condition
the policy already guarantees.

**If a future page needs post-load settling** — most plausibly a `data:`-URI
`@font-face`, since `load` does not guarantee `document.fonts.ready` — add an
explicit wait for *that specific thing* inside the same `withDeadline` closure.
Do not reach back for `networkidle0`.

### Fixed wall-clock guesses failed three times on this branch

| Where | The guess | Fix |
| --- | --- | --- |
| `TIMEOUT_PAGE_LOAD_MS` doubling as `protocolTimeout` | 30 s | split the constants; own deadline |
| `process-leak.test.mjs` SIGKILL grace | 4 s | poll to condition, 15 s cap |
| `studio.test.mjs` process-table wait | 700 ms | poll to condition + warm PowerShell |

The third is the instructive one. The first poll fix **still went red** — its own
failure message showed `readCommandLine` never succeeded once in 15 s, because it
spawns PowerShell under a 5 s `spawnSync` cap and cold start on a contended runner
exceeds it. Every call was killed before the file cache warmed, so every call was
the first call. No polling deadline escapes that; the fix pays the cold start once,
outside the cap.

A failure message that named *what was missing* is what produced the correct
diagnosis.

---

## 6. The working method, if you continue this way

Every gate on this branch was proven against a known-bad input before being
trusted. That caught four real defects that would otherwise have shipped as
green:

- ESLint's `languageOptions` silently clobbered by object-spread ordering
- a bundle verifier whose negative test picked the one case already covered
- an extract-directory guard that deleted files it did not own — including a
  signature matching this repo's own `build/` staging tree
- two version-sync tests that asserted *something* threw, not *what*

One refinement the work produced: **a guard's proof must include the input that
makes it permit, not only the input that makes it refuse.** Both of Task 6's
findings lived in that half.
