# Normalise to LF in the repository. Without this the Windows checkout commits
# CRLF and the macOS/Linux CI legs see every file as modified.
* text=auto eol=lf

# Binary, never touch.
*.png binary
*.mcpb binary
